// Type the parameters below as numbers:

// export const addTwoNumbers = (a, b) => {
//     return a + b;
//   };

// Solution:
export const addTwoNumbers = (a: number, b: number) => {
  return a + b;
};

// Options other than "number" include "string" and "boolean".